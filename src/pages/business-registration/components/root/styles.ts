import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  page: {
    position: 'relative',
    padding: '40px 16px 16px',
  },
  progress: {
    position: 'absolute',
    width: '100%',
    left: 0,
    top: 0,
  },
});
