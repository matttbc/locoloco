import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  loginButton: {
    display: 'block',
    marginTop: '20px',
    marginLeft: 'auto',
    marginRight: 'auto',
  },
});
